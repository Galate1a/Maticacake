[phases.setup]
nixPkgs = ["php", "phpExtensions.intl", "phpExtensions.zip", "phpExtensions.bcmath"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"